using UnityEngine;
using System.Collections;

public class Enemy : MonoBehaviour 
{
	public int health;	
	private float xx;
	public bool yo;

    /*
	void Start()
	{
		xx = this.transform.position.x;
	}

    void OnTriggerEnter2D (Collider2D other)
    {
       
    }

	void Update()
	{
		if (yo == true) xx += 0.01f;
		this.transform.position = new Vector3 (xx, this.transform.position.y);
	}
    */
}